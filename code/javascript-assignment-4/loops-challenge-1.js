const wordList = ["every", "word", "in", "this", "array", "should", "be", "capitalized"]
for (list of wordList) {
      const touppercaselist = wordList.map(list => 
          list.toUpperCase());
      console.log(touppercaselist);
}
