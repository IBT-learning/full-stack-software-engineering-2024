import { createContext } from "react"

// This is a "context" object that will keep track of state
// across the whole app it can be accessed by any component
// that it's provided to
export const UserContext = createContext("")
