// Add this file to the `src/` directory and configure it in `vite.config.js`.

import '@testing-library/jest-dom';

// Optional: If you use React Router or other libraries that need mocking, configure them here.
// For example, if using React Router:
// import { MemoryRouter } from 'react-router-dom';
