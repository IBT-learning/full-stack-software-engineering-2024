// Strings
const myName = "Adam";
console.log(myName.length);
console.log("Hello, my name is " + myName);
console.log(`When my friends see me they shout ${myName.toUpperCase()}!`);

// Numbers
const challenge2 = (((24 * 2 + 8) / 2) - 24);
console.log(challenge2);

//area of a circle with a radius of 2
const radius = 2
const area = (Math.PI * (radius ** 2))
console.log(area.toFixed(4));

// area of a circle with a radius of 3
const radius2 = 3
const area2 = (Math.PI * (radius2 ** 2))
console.log(area2.toFixed(4));